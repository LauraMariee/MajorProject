using UnityEngine;

namespace Filtering.Buttons
{
    public class ColourDetails : MonoBehaviour
    {
        public string buttonName{ get; set; }
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
